import { Component } from '@angular/core';

/**
 * Registered workflow definitions. A placeholder until the definitions view.
 */
@Component({
  selector: 'app-workflow-list',
  template: `<h1 i18n>Workflows</h1>
    <p i18n>The workflow list arrives with a later story.</p>`,
})
export class WorkflowList {}